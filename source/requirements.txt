# Залежності для source/dataset_scripts/ (PowerGraph: .mat, візуалізація).
# source/mimd-pc/app.py — лише стандартна бібліотека Python, pip не потрібен.
#
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r source/requirements.txt

numpy
scipy
h5py
matplotlib
networkx
